{
  "name": "agent-policy",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "bun test",
    "build": "bun build ./src/cli.ts --target=node --format=esm --outfile=./dist/agent-policy.mjs --banner='#!/usr/bin/env node'",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "verify:dist": "node ./scripts/verify-dist.mjs"
  }
}
